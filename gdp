import wbdata
import pandas as pd
country="IN"
indicator={"NY.GDP.MKTP.CD":"GDP(USD)"}
df=wbdata.get_dataframe(indicator,country=country)
df=df.reset_index()
df=df.dropna(subset=["GDP(USD)"])
print(df)
